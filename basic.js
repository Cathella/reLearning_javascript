// Javascript datatypes
// undefined, null, symbol, number, bigint, object, string, boolean

// Declare JavaScript Variables
var myName;

// Storing Values with the Assignment Operator
var a;
a = 7;

// Assigning the Value of One Variable to Another
var a;
a = 7;
var b;
b = a;

// Initializing Variables with the Assignment Operator
var a = 9;

// Understanding Uninitialized Variables
var a = 5; // When JavaScript variables are declared,
var b = 10; //  they have an initial value of undefined
var c = "I am a";

a = a + 1; // If you do a mathematical operation on an undefined 
b = b + 5; // variable your result will be NaN - "Not a Number"
c = c + " String!"; // If you concatenate a string with an undefined variable, 
// you will get a literal string of undefined

// Understanding Case Sensitivity in Variables
var studlyCapVar;
var properCamelCase;
var titleCaseOver; // Write variable names in JavaScript in camelCase

studlyCapVar = 10;
properCamelCase = "A String";
titleCaseOver = 9000;

// Add Two Numbers with JavaScript
var sum = 10 + 10;

// Subtract One Number from Another with JavaScript
var difference = 45 - 33;

// Multiply Two Numbers with JavaScript
var product = 8 * 10;

// Divide One Number by Another with JavaScript
var quotient = 66 / 33;

// Increment a Number with JavaScript
var myVar = 87;
myVar++;

// Decrement a Number with JavaScriptPassed
var myVar = 11;
myVar--;

// Create Decimal Numbers with JavaScript
var myDecimal = 5.8;

// Multiply Two Decimals with JavaScript
var product = 2.0 * 2.5;

// Divide One Decimal by Another with JavaScript
var quotient = 4.4 / 2.0;

// Finding a Remainder in JavaScript
var remainder = 11 % 3;

// Compound Assignment With Augmented Addition
var a = 3;
var b = 17;
var c = 12;
a += 12;
b += 9;
c += 7;

// Compound Assignment With Augmented Subtraction
var a = 11;
var b = 9;
var c = 3;
a -= 6;
b -= 15;
c -= 1;

// Compound Assignment With Augmented Multiplication
var a = 5;
var b = 12;
var c = 4.6;
a *= 5;
b *= 3;
c *= 10;

// Compound Assignment With Augmented Division
var a = 48;
var b = 108;
var c = 33;
a /= 12;
b /= 4;
c /= 11;

